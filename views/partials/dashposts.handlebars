<div class="container">
    <div class="card mb-6" style="margin-bottom: 30px; border-radius: 0px 40px 40px 0px;">
        <header class="card-header" style="border-radius: 0px 40px 0px 0px;">
            <div class="col">
                <p class="card-title"><a href="/post/{{id}}" class="btn btn-sm">{{name}}</a></p>
            </div>
        </header>
        <div class="card-body">
            <div class="card-text">{{body}}</div>
            <a href="/dashboard/update/{{id}}"><button class="btn btn-sm">Update</button></a>
        </div>
    </div>
</div>